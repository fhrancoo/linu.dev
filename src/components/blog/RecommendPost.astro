---
const {
	Title,
	Url,
	Description,
} = Astro.props
---
<div class='flex flex-col items-start place-content-center mx-auto max-w-xl not-prose font-mona-sans my-10 text-text'>
	<a href={Url} class='px-8 py-6 mx-auto bg-gradient-purple rounded-xl no-underline flex flex-col gap-2 transition-all ease-in-out duration-500 ring-2 ring-transparent hover:ring-astro-blue'>
		<h3 class='text-xl font-extrabold'>{Title}</h3>
		<p class='text-sm font-medium'>{Description}</p>
	</a>
	<span class='text-xs text-subtle/60 font-medium px-2 py-2'>Post relacionado</span>
</div>
